# frozen_string_literal: true
source "https://rubygems.org"

# Lint files in local development only
gem "rubocop" unless ENV["CI"]

# For Travis
gem "rake", "~> 11.0"

# All gem dependencies are specified in particlepi.gemspec
gemspec
